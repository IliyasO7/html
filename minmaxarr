let arr = [3, 5, 4, 1, 9];

let min=arr[0];
let max = arr[0];

for(var i=0;i<arr.length;i++)
{
    if(max<arr[i])
    {
        max=arr[i];
    }
    if(min>arr[i])
    {
        min=arr[i];
    }

}
console.log("MIN IS:"+min +","+"MAX IS:"+ max);
